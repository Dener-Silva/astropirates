export enum ClientTopic {
    "SetName",
    "Input"
}

export enum ServerTopic {
    "SetID",
    "UpdateGameState"
}

export enum FirstName {
    "Blue",
    "Red"
}

export enum SecondName {
    "Cat",
    "Dog"
}